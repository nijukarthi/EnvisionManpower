import { FormFieldError } from './form-field-error';

describe('FormFieldError', () => {
  it('should create an instance', () => {
    const directive = new FormFieldError();
    expect(directive).toBeTruthy();
  });
});
